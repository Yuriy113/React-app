let initialState = {
  friends: [
    { id: 1, name: 'Anrdew' },
    { id: 1, name: 'Sasha' },
    { id: 1, name: 'Sveta' },
  ]
};

const friendsPageReducer = (state = initialState, action) => { 
  return state;
}

export default friendsPageReducer;